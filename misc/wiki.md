1. detect the runtime versions that are being requested by the current project, 
2. activate the requested versions if I already have them installed, and 
3. warn me if the project is requesting a runtime version that isn't installed.
